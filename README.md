# Bank-Transaction-Analysis
This project is a Bank Transaction Analysis System built using SQL. It manages and analyzes banking transaction data by creating multiple related tables such as customers, cards, merchants, and transactions. The project uses SQL concepts like joins, subqueries, aggregate functions, derived tables, and views to generate meaningful business insights.
